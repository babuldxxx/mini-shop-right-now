# mini-shop-right-now

# Run and Stop the Medusa Application Running in Docker
npm run docker:up 
npm run docker:down

# Run storefront
cd right-now-storefront
npm run dev

# Check logs
docker compose logs -f

